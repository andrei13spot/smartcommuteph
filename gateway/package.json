{
  "name": "smartcommuteph-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "node web + map gateway for smartcommute ph. serves the frontend and the map api, proxies routing to the python engine.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}

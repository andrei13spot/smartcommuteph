fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
numpy>=2.0
scipy>=1.13
pytest==8.3.4
scikit-learn>=1.5
joblib>=1.4
# tensorflow is optional: the lstm falls back to the data-derived curve without
# it. install manually if you want to retrain: pip install tensorflow-cpu
